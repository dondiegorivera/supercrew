# .github/workflows/ci.yml
# Basic CI: runs on pull requests to main.
# TODO: Add your project's build, test, and lint steps.

name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # TODO: Add your language/runtime setup. Examples:
      #
      # - uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.12'
      #
      # - uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'

      # TODO: Add your build step
      # - name: Build
      #   run: make build

      # TODO: Add your test step
      # - name: Test
      #   run: make test

      # TODO: Add your lint step
      # - name: Lint
      #   run: make lint
